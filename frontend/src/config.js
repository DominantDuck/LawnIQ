/** Same-origin `/api` — Next.js routes by default; Express only if USE_EXPRESS_BACKEND=true. */
export const API_BASE_URL = '/api';

/** Shown in UI when config fails and Express proxy mode is expected */
export const BACKEND_DEV_URL = 'http://127.0.0.1:3001';
